#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	num = 42;

	printf("The number is %d/n", num);
	printf("The number is %d/n", num);

	return (0);
}
